# Note
This repo previously hosted an old Netlify webhook function at netlify/functions/webhook.js. That implementation has been deprecated and replaced with a stub that returns 410 Gone to prevent accidental invocation.

If you want the file permanently deleted from the repository history, remove it using the GitHub web UI (Delete file) or via git locally and push the change.
